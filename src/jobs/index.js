import { useCmd } from "./useCmd.js";
import { useFile } from "./useFile.js";
import { useArtifact } from "./useArtifact.js";

export const jobs = {
    "useCmd": useCmd,
    "useFile": useFile,
    "useArtifact": useArtifact
}